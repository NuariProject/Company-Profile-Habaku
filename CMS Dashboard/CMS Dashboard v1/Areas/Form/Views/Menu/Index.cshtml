@model CMS_Dashboard_v1.Models.ModelForm.MenuModel

@{
    ViewBag.title = "Master Menu";
}

<h3 class="fw-bold py-0 mb-4 "><span class="fw-light">Pages / </span> @ViewBag.Title</h3>

<div class="row">
    <div class="col-sm-12">
        <div class="card">
            @*StartTittle*@
            <div class="card-body">
                <div class="row">
                    <div class="col">
                        <a asp-controller="Menu" asp-action="Create" type="button" class="btn btn-primary">
                            <i class="fas fa-plus-square"></i>
                            <span>
                                &nbsp; Buat Data
                            </span>
                        </a>
                    </div>
                </div>
            </div>

            <hr class="my-0" />
            @*EndTittle*@
            <div class="card-body">
                <div class="table-responsive">
                    <table id="datatable" class="table table-hover" data-toggle="data-table">
                        <thead>
                            <tr>
                                <th width="80%">Menu</th>
                                @*<th>Status</th>*@
                                <th style="text-align:center;">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.List)
                            {
                                <tr>
                                    <td>@item.menu_name</td>
                                    @*<td>@item.status</td>*@
                                    <td style="text-align:center;">
                                        <a asp-controller="Menu" asp-action="Edit" asp-route-id="@item.menu_id" type="button" class="btn btn-icon btn-warning">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a asp-controller="Menu" asp-action="Delete" asp-route-id="@item.menu_id" type="button" class="btn btn-icon btn-danger">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>

                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
